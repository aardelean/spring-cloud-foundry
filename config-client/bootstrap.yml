spring:
  application:
    name: eureka
  cloud:
    config:
      uri: http://localhost:8888/
  memory: 512MB
  buildpack: java_buildpack
  path: build/libs/eureka-server-1.0.jar
  host: cloud-es
  profiles:
      active: dev
